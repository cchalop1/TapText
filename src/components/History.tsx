import React, { useState, useEffect } from 'react';

interface Props { }

export const History: React.FC<Props> = (props) => {
    // TODO : list all history and fech fron localstorage
    return null;
}